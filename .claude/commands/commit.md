Docs/CommitGuide.md 파일을 읽고 그 규칙에 따라 커밋 메시지를 추천해 줘.

1. 먼저 `git diff`와 `git diff --staged`를 실행해서 현재 변경분을 확인해.
2. CommitGuide.md의 모든 규칙(타입 선택, 제목 형식, 본문 형식, 글자 수 제한 등)을 준수해.
3. 목적이 다른 변경이 섞여 있으면 커밋을 나눠서 각각 메시지를 제안해.
4. 최종 제안은 코드 펜스 블록으로 감싸서 복사하기 쉽게 제시해.
